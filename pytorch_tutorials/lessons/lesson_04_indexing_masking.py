# 第 04 课：索引、切片与布尔 mask
#
# 核心讲解
# 这里保留最短版本，详细教程请先读：
# ../docs/tutorial_04_indexing_masking.md
#
# 1. 索引用来取局部数据，切片常用于 batch、序列、图像区域。
# 2. 布尔 mask 可以筛选满足条件的元素，是处理异常值、条件统计的常用手段。
# 3. 训练中经常会按标签、置信度、padding mask 来选择样本或位置。
#
# 操作路线
# 写本节代码时，可以按这个顺序推进：
# 1. 创建 4x5 的矩阵。
# 2. 取出第 2 行、前 3 列、最后一列。
# 3. 用 mask 找出所有大于 10 的元素，并计算它们的均值。
#
# 小检查
# - 先把关键张量 print 出来，确认数值和 shape 符合预期。
# - 如果报错，优先检查 shape、dtype、device 这三件事。
# - 如果训练结果不动，优先检查是否 zero_grad、backward、step 都写了。
#
# 学习目标
# - 能用自己的话解释本节核心概念。
# - 能不看答案写出一个可以运行的最小 PyTorch 程序。
# - 能通过输出的 shape、loss 或 accuracy 判断代码是否基本正确。
#
# 练习任务
# - 创建 4x5 的矩阵。
# - 取出第 2 行、前 3 列、最后一列。
# - 用 mask 找出所有大于 10 的元素，并计算它们的均值。
#
# 做题要求
# - 尽量先不看 answers 目录。
# - 代码需要能直接运行。
# - 打印关键 shape、loss 或 accuracy，确认自己真的跑通了。
#
# ===== 练习区：从这里开始写代码 =====
def main():
    # TODO: 练习索引、切片和布尔 mask。
    pass


if __name__ == "__main__":
    main()
