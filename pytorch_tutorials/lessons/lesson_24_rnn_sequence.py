# 第 24 课：RNN：序列分类入门
#
# 核心讲解
# 这里保留最短版本，详细教程请先读：
# ../docs/tutorial_24_rnn_sequence.md
#
# 1. RNN 会按时间步处理序列，隐藏状态携带之前的信息。
# 2. 输入形状常用 [batch, time, features]，需要设置 batch_first=True。
# 3. 序列分类常取最后一个时间步的隐藏状态接分类层。
#
# 操作路线
# 写本节代码时，可以按这个顺序推进：
# 1. 生成长度为 8 的一维序列。
# 2. 标签为序列后半段均值是否大于前半段均值。
# 3. 使用 nn.RNN 做二分类。
#
# 小检查
# - 先把关键张量 print 出来，确认数值和 shape 符合预期。
# - 如果报错，优先检查 shape、dtype、device 这三件事。
# - 如果训练结果不动，优先检查是否 zero_grad、backward、step 都写了。
#
# 学习目标
# - 能用自己的话解释本节核心概念。
# - 能不看答案写出一个可以运行的最小 PyTorch 程序。
# - 能通过输出的 shape、loss 或 accuracy 判断代码是否基本正确。
#
# 练习任务
# - 生成长度为 8 的一维序列。
# - 标签为序列后半段均值是否大于前半段均值。
# - 使用 nn.RNN 做二分类。
#
# 做题要求
# - 尽量先不看 answers 目录。
# - 代码需要能直接运行。
# - 打印关键 shape、loss 或 accuracy，确认自己真的跑通了。
#
# ===== 练习区：从这里开始写代码 =====
def main():
    # TODO: 用 nn.RNN 完成序列分类。
    pass


if __name__ == "__main__":
    main()
