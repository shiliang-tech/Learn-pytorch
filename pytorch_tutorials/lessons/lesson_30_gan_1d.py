# 第 30 课：GAN：一维分布生成
#
# 核心讲解
# 这里保留最短版本，详细教程请先读：
# ../docs/tutorial_30_gan_1d.md
#
# 1. GAN 包含生成器 G 和判别器 D，二者交替训练。
# 2. D 学会区分真实样本和生成样本，G 学会骗过 D。
# 3. GAN 训练不稳定，本节只做最小可运行例子，理解训练步骤即可。
#
# 操作路线
# 写本节代码时，可以按这个顺序推进：
# 1. 真实数据来自均值 2、标准差 0.5 的一维高斯。
# 2. Generator 把噪声映射成一维样本。
# 3. 交替训练 D 和 G，并打印生成样本均值。
#
# 小检查
# - 先把关键张量 print 出来，确认数值和 shape 符合预期。
# - 如果报错，优先检查 shape、dtype、device 这三件事。
# - 如果训练结果不动，优先检查是否 zero_grad、backward、step 都写了。
#
# 学习目标
# - 能用自己的话解释本节核心概念。
# - 能不看答案写出一个可以运行的最小 PyTorch 程序。
# - 能通过输出的 shape、loss 或 accuracy 判断代码是否基本正确。
#
# 练习任务
# - 真实数据来自均值 2、标准差 0.5 的一维高斯。
# - Generator 把噪声映射成一维样本。
# - 交替训练 D 和 G，并打印生成样本均值。
#
# 做题要求
# - 尽量先不看 answers 目录。
# - 代码需要能直接运行。
# - 打印关键 shape、loss 或 accuracy，确认自己真的跑通了。
#
# ===== 练习区：从这里开始写代码 =====
def main():
    # TODO: 训练一个极小的一维 GAN。
    pass


if __name__ == "__main__":
    main()
