# 第 01 课：Tensor 入门：创建、运算、矩阵乘法
#
# 核心讲解
# 这里保留最短版本，详细教程请先读：
# ../docs/tutorial_01_tensor_basics.md
#
# 1. Tensor 是 PyTorch 的核心数据结构，可以把它理解成支持 GPU 和自动求导的多维数组。
# 2. 标量是 0 维，向量是 1 维，矩阵是 2 维，更高维常用来表达 batch、通道、图像高宽、时间步等。
# 3. 深度学习里的大多数计算最终都会落到张量运算：加减乘除、矩阵乘法、聚合统计。
#
# 操作路线
# 写本节代码时，可以按这个顺序推进：
# 1. 创建一个 2x3 的浮点张量 x。
# 2. 创建一个 3x2 的浮点张量 w，并计算 x @ w。
# 3. 打印结果的 shape、均值、最大值。
#
# 小检查
# - 先把关键张量 print 出来，确认数值和 shape 符合预期。
# - 如果报错，优先检查 shape、dtype、device 这三件事。
# - 如果训练结果不动，优先检查是否 zero_grad、backward、step 都写了。
#
# 学习目标
# - 能用自己的话解释本节核心概念。
# - 能不看答案写出一个可以运行的最小 PyTorch 程序。
# - 能通过输出的 shape、loss 或 accuracy 判断代码是否基本正确。
#
# 练习任务
# - 创建一个 2x3 的浮点张量 x。
# - 创建一个 3x2 的浮点张量 w，并计算 x @ w。
# - 打印结果的 shape、均值、最大值。
#
# 做题要求
# - 尽量先不看 answers 目录。
# - 代码需要能直接运行。
# - 打印关键 shape、loss 或 accuracy，确认自己真的跑通了。
#
# ===== 练习区：从这里开始写代码 =====
def main():
    # TODO: 创建 x 和 w，然后完成矩阵乘法。
    pass


if __name__ == "__main__":
    main()
