# 第 19 课：CNN 小项目：识别条纹方向
#
# 核心讲解
# 这里保留最短版本，详细教程请先读：
# ../docs/tutorial_19_cnn_synthetic_images.md
#
# 1. CNN 适合处理局部空间模式，例如边缘、纹理、形状。
# 2. 合成数据能帮助你先掌握训练流程，不依赖下载数据集。
# 3. Flatten 前要确认卷积输出形状，避免 Linear 的输入维度写错。
#
# 操作路线
# 写本节代码时，可以按这个顺序推进：
# 1. 生成 16x16 灰度图：竖条纹为 0 类，横条纹为 1 类。
# 2. 写一个小 CNN 分类器。
# 3. 训练并打印准确率。
#
# 小检查
# - 先把关键张量 print 出来，确认数值和 shape 符合预期。
# - 如果报错，优先检查 shape、dtype、device 这三件事。
# - 如果训练结果不动，优先检查是否 zero_grad、backward、step 都写了。
#
# 学习目标
# - 能用自己的话解释本节核心概念。
# - 能不看答案写出一个可以运行的最小 PyTorch 程序。
# - 能通过输出的 shape、loss 或 accuracy 判断代码是否基本正确。
#
# 练习任务
# - 生成 16x16 灰度图：竖条纹为 0 类，横条纹为 1 类。
# - 写一个小 CNN 分类器。
# - 训练并打印准确率。
#
# 做题要求
# - 尽量先不看 answers 目录。
# - 代码需要能直接运行。
# - 打印关键 shape、loss 或 accuracy，确认自己真的跑通了。
#
# ===== 练习区：从这里开始写代码 =====
def main():
    # TODO: 生成条纹图像，训练 CNN 做二分类。
    pass


if __name__ == "__main__":
    main()
