# 第 05 课：聚合统计、范数与标准化
#
# 核心讲解
# 这里保留最短版本，详细教程请先读：
# ../docs/tutorial_05_reductions_norms.md
#
# 1. mean、sum、max、argmax 这类 reduction 会沿某些维度汇总信息。
# 2. 范数常用来度量向量大小，训练中也会用于正则化、梯度裁剪、相似度计算。
# 3. 标准化让数据均值接近 0、标准差接近 1，通常能让优化更稳定。
#
# 操作路线
# 写本节代码时，可以按这个顺序推进：
# 1. 创建一个 5x3 的随机数据矩阵。
# 2. 计算每一列的均值和标准差。
# 3. 把每一列标准化，并验证标准化后的均值接近 0。
#
# 小检查
# - 先把关键张量 print 出来，确认数值和 shape 符合预期。
# - 如果报错，优先检查 shape、dtype、device 这三件事。
# - 如果训练结果不动，优先检查是否 zero_grad、backward、step 都写了。
#
# 学习目标
# - 能用自己的话解释本节核心概念。
# - 能不看答案写出一个可以运行的最小 PyTorch 程序。
# - 能通过输出的 shape、loss 或 accuracy 判断代码是否基本正确。
#
# 练习任务
# - 创建一个 5x3 的随机数据矩阵。
# - 计算每一列的均值和标准差。
# - 把每一列标准化，并验证标准化后的均值接近 0。
#
# 做题要求
# - 尽量先不看 answers 目录。
# - 代码需要能直接运行。
# - 打印关键 shape、loss 或 accuracy，确认自己真的跑通了。
#
# ===== 练习区：从这里开始写代码 =====
def main():
    # TODO: 完成按列标准化。
    pass


if __name__ == "__main__":
    main()
